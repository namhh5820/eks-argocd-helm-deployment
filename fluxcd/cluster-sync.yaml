apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: apps-sync
  namespace: flux-system
spec:
  interval: 1m
  path: "./fluxcd"
  prune: true
  sourceRef:
    kind: GitRepository
    name: main-repo
  targetNamespace: flux-system
